#name: React Vite app CI
#
##1 Triggers
#
#on:
#    push:
#        branches: [main, master, feature/*]
#    pull_request:
#        branches: [main, master, feature/*]
#
##2 Jobs
#jobs:
#    build-and-test:
#        runs-on: ubuntu-latest
#
#        defaults:
#            run:
#                working-directory: ./BookLibrary/booklibrary.client
#        steps:
#            - name: "Checkout repository - lol"
#              uses: actions/checkout@v5 # v5 already published
#
#            - name: "Set up Node.js"
#              uses: actions/setup-node@v5
#              with:
#                  node-version: 24
#                  cache: "npm"
#
#            - name: "Install dependecies"
#              run: npm ci
#
#            - name: "Run linter"
#              run: npm run lint --if-present
#
#            - name: "Run tests"
#              run: npm test --if-present
#
#            - name: "Build project"
#              run: npm run build
